EXPLAIN("A PromptDesk in the prefab list is necessary for an example environment");
CREATE("PromptDesk");
EXPLAIN("The player asks to place objects in the corner, and PromptCorner1 is the most top corner in head_stay_frames.Put the PromptDesk to the PromptCorner1 but consider PromptWallZNegative and PromptWallXNegative. Since Player doesn't specify, I decide the PromptDesk aligns with PromptWallZNegative. Since no mathematical expression is allowed, I must calculate by myself. Given PromptWallZNegative is at z = 9.92, PromptDesk should have z = 9.92 - 0.88 / 2 = 9.48, since it has size z = 0.88. Given PromptWallXNegative is at x = 9.94, PromptDesk should have x = 9.94 - 1.72 / 2 = 9.08, since it has size x = 1.72. Since PromptDesk aligns with PromptWallZNegative, it should have the same forward direction with the wall, which is z = 1.");
MOVE("crt", 9.08, 0.05, 9.48);
FORWARD("crt", z=1);
EXPLAIN("Same to PromptDesk, a PromptChair is necessary for an example environment.");
CREATE("PromptChair");
EXPLAIN("Since no mathematical expression is allowed, I must calculate by myself. Consider the PromptChair has boundary size z = 0.72, PromptDesk has boundary size z = 0.88, the minimum distance between them should be (0.72 + 0.88) / 2 = 0.8, provide 0.2 extra space, the z = 9.48 - 0.8 - 0.2 = 8.48, and x follows with the PromptDesk. Set the look at point as the PromptDesk's x and z position to face the PromptChair to it.");
MOVE("crt", 9.08, 0.05, 8.58);
LOOKAT("crt", x=9, z=9.5);
EXPLAIN("PromptCactus contributes to an example environment");
CREATE("PromptCactus");
EXPLAIN("Consider the size of the table (1.72, 0.74, 0.88), and the PromptCactus is unlikely to be placed at the center of the PromptDesk, I decide to add 0.5 offset on the x, and 0.2 on the y, so that the PromptCactus will be on a corner of the PromptDesk.Since no mathematical expression is allowed, I must calculate by myself.  That will be x = 9.08 - 0.5  = 8.58, z = 9.48 + 0.2 = 8.58, and y = 0.05 + 0.74 = 0.79.");
MOVE("crt", 8.58, 0.79, 8.58);
EXPLAIN("PromptPicture contribute to an example environment");
CREATE("PromptPicture");
EXPLAIN("Hang the PromptPicture on PromptWallZNegative above the PromptDesk. Given PromptWallZNegative is at z = 9.92, set z of the PromptPicture to it. And set the PromptDesk's x to the PromptPicture, while make sure PromptPicture is Parallel to PromptWallZNegative by assigning PromptWallZNegative's forward direction.");
MOVE("crt", 9.08, 1.65, 9.48);
FORWARD("crt", z=-1);
EXPLAIN("Create a PromptBookShelf as asked by the player to match the hit point.");
CREATE("PromptBookShelf");
EXPLAIN("The hit point <h-1> is at PromptWallXNegative but PromptBookShelf should on the Floor, so the player want the PromptBookShelf to align with PromptWallXNegative roughly at the hit point.Since no mathematical expression is allowed, I must calculate by myself.  Set the x of it via x = 9.94 - 1.17 / 2 = 9.36, z follows the hit point z = 6.18, and set y at the Floor y = 0.05. Since PromptWallXNegative has forward direction z = -1, set it to the PromptBookShelf");
MOVE("crt", 9.36, 0.05, 6.18);
FORWARD("crt", x=-1);
EXPLAIN("The rotation of the PromptBookShelf varied from the default, leading to a boundary update, now the PromptBookShelf is not closely align PromptWallXNegative. Reposition is need, reconsidering the boundary size. Since no mathematical expression is allowed, I must calculate by myself. After rotation, the z direction of the PromptBookShelf matches with PromptWallXNegative's forward, and the z = 0.45, therefore, the new x position should be x = 9.94 - 0.45 / 2 = 9.72.");
MOVE("crt", 9.72, 0.05, 6.18);
EXPLAIN("Since no mathematical expression is allowed, I must calculate by myself. Move the PromptCouch along its forward direction by 1: z = 6 + (-1) = 5");
MOVE("-000000", z=5);
